<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:PokemonEssentialsEditorEvs.ViewModels"
             x:Class="PokemonEssentialsEditorEvs.Views.ProjectView"
             x:DataType="vm:MainWindowViewModel">
    
    <Grid Background="#1E1E1E">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
            <TextBlock Text="HAWAII" FontSize="32" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
            <TextBlock Text="Selecciona la carpeta de tu juego para comenzar a editar." Foreground="#AAAAAA" HorizontalAlignment="Center"/>
            
            
            <Button Content="📁 Abrir Proyecto" 
                    Click="SelectProjectFolder_Click"
                    FontSize="16" FontWeight="Bold" 
                    Background="#007ACC" Foreground="White" 
                    Padding="20,10" CornerRadius="5"
                    HorizontalAlignment="Center" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</UserControl>